{
  "name": "shopping-pet-root",
  "version": "1.0.0",
  "description": "Pet Shop E-commerce - Root Directory",
  "scripts": {
    "install:all": "npm.cmd install && cd backend && npm.cmd install && cd ../frontend && npm.cmd install"
  }
}
